const initState = {
    hoteli: []
}

const hoteliReducer = (state = initState, action) => {
    switch(action.type){
        case 'GET_FILTERED_HOTELS':
            
    }
    return state
}

export default hoteliReducer;